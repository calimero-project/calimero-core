/**
 * Contains translators for translating between KNX datapoint types (DPT), java types and values carrying more specific
 * type semantics, e.g., engineering unit.<br>
 * {@link io.calimero.dptxlator.PropertyTypes} provides a mapping of property data type (PDT), for associating
 * property types to DPT translators.
 * {@link io.calimero.dptxlator.TranslatorTypes} provides lookup support for available DPT translators when
 * working with datapoint types.
 */

package io.calimero.dptxlator;
